import 'package:flutter/material.dart';

class StateWindow extends StatelessWidget {
  const StateWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
